# セキュリティ

pd は Claude Code の拡張機能として、**利用者の手元で自動的にコードを実行します**（hook）。そのため、ここで扱う問題は「このリポジトリのファイルが利用者の環境に何をするか」が中心になります。

---

## 報告する

**公開の Issue には書かないでください。** 修正が配られる前に手口が公開されると、更新していない人が危険にさらされます。

[**Security → Report a vulnerability**](https://github.com/monkeikawashima/pd-plugin/security/advisories/new) から報告してください。報告はメンテナにだけ見える形で届きます。

- **1週間以内**に受け取ったことを返します
- 修正の見込み・配る版・公開の時期を、そこで相談します
- 修正を配ったあと、報告内容を Advisory として公開します（希望があれば名前を載せます。不要なら伏せます）

返信が無い場合は、同じ画面から催促してください。**メンテナは1人です。**

---

## 対象になる版

**最新の版だけ**です。過去の版へ修正を戻すことはしません。

更新は自動では届きません（Claude Code は公式以外の配布元を既定で自動更新しないため）。`/pd:update` を実行した人にだけ届きます。詳しくは [README](README.md#更新する)。

---

## 対象になるもの

配布物が、利用者の意図しない動作を起こす場合。

- `hooks/` — 利用者の**全プロジェクトで毎ターン走る**部分
- `scripts/` — `validate.py` / `hook.py` / `update_check.py` など、hook から呼ばれるもの
- `commands/` — `/pd:init` `/pd:validate` `/pd:uninstall` が実行する手順
- `skills/` — 意図しないファイルの読み書きや外部送信を促す記述

具体的には、次のようなものです。

- 配布物経由で任意のコードが実行される
- 利用者のファイルを、意図しない範囲で読む・書く・消す
- `pd/.local/`（元データの置き場所）の中身が、意図せず外部へ出る
- 更新の確認や配布の経路を乗っ取れる

---

## 対象にならないもの

**利用者が自分で置いたデータの扱い。** `pd/.local/` は共有されない設定で作られますが、[README に書いているとおり](README.md#元データの置き場所だけ注意)、**フォルダごとクラウドストレージで同期していれば中身も一緒に渡ります。** 同期からの除外は利用者側の設定です。

- Claude Code 本体・MCP サーバ・Notion / Drive / Slack など外部サービス側の問題（それぞれの提供元へ報告してください）
- 利用プロジェクトの分析データそのものの内容
- `pd/.local/` に個人情報を置いたまま公開リポジトリへコミットした、といった運用の結果

---

## 設計上そうなっている挙動

**脆弱性ではありませんが、知らないと驚くもの**を挙げます。

**hook は、有効化した全プロジェクトで走ります。** ただし目印（`$CLAUDE_PROJECT_DIR/pd/ledger.json`）が無ければ**何もせず終了します**。pd と無関係なリポジトリで検証器は動きません。

**起動時に更新の有無を確認します。** `api.github.com` へ問い合わせ、最新の版番号を読みます。

- 送るのは GET のみです。**プロジェクトの内容・ファイル名・利用者の情報は一切送りません**（付けるヘッダは `Accept` と `User-Agent: pd-plugin-update-check` の2つだけ）
- 問い合わせは**6時間に1回まで**。タイムアウトは8秒
- 通信は裏で行い、失敗しても作業を止めません。**オフラインでも pd は動きます**
- 結果は `~/.cache/pd-plugin/update-check.json`（Windows は `%LOCALAPPDATA%\pd-plugin\`）に残ります。**pd がプロジェクトの外に書くのは、このファイルだけです**
- 止めたい場合は `hooks/hooks.json` の SessionStart を外してください。ただし `/plugin update` で plugin ごと入れ替わるため、**更新のたびに戻ります**

**依存は `python3` だけです。** 外部パッケージは使いません（Windows でも動かすため、また依存の脆弱性を持ち込まないためです）。

---

## 見つけたけれど脆弱性か分からないとき

**判断に迷うなら、非公開の報告で送ってください。** 違えば通常の Issue へ移します。逆は取り返しがつきません。
