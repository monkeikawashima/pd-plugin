---
id: US-NN
status: 起草 | 合意済み | 要再検証
updated: YYYY-MM-DD
---

# US-NN — <一行タイトル>

## ストーリー（書式厳守）

```
〈アクター〉は、〈対象〉を〈できる〉。なぜなら〈目的〉したいから
```

> <ここに1文で書く>

| 要素 | 書き方 | 禁止 |
|---|---|---|
| アクター | 役割名で書く | 「ユーザー」で済ませる |
| 対象 | **オブジェクト名**（`pd/specs/03-structure/object-model.md` の定義） | 画面名・機能名 |
| できる | 操作を書く | 機能名（「〜機能を使える」） |
| なぜなら | **目的** | 手段の言い換え |

## 属性

| 項目 | 値 |
|---|---|
| 対象オブジェクト | <定義済み / **新規候補**（→ `object-model-reviewer`）> |
| フェーズ | <定義済み / 未定義> |
| 効く体験の原則 | <番号。**紐づかないものは要件に入れない**> |

## コンセプト還元

<コンセプト文のどの要素に効くか。一文で>

## 根拠

| 種別 | 内容 |
|---|---|
| 根拠ボイス | VOICE-xxx / **なし（デザイナー起案）** |

> 根拠ボイスが無いものを「顧客要望」と書かない。**無いなら「デザイナー起案」と明記する。**

## 実装状況

| 判定 | 判定根拠 |
|---|---|
| 実装済み / 部分実装 / 不足 | 実機画面 / コード（パス） / **未確認** |

> `実装済み` は**機能の有無**の判定。品質が要求を満たすかは別問題。

## 関連

- 重複・矛盾する既存ストーリー: <ID / 突合済みで無し>
- 関連 UXDR: <UXDR-…>

---

## 記入例

```
運用者は、届いた申込を承認できる。なぜなら枠を確保して当日の準備を始めたいから
```

| 項目 | 値 |
|---|---|
| 対象オブジェクト | 申込 |
| フェーズ | 未定義（構造層で確定予定） |
| 効く体験の原則 | 未定義 |
| コンセプト還元 | 未定義（戦略層が空のため書けない → **要件に昇格させない**） |
| 根拠ボイス | なし（デザイナー起案） |
| 実装状況 | 実装済み（判定根拠: 実機画面 `<パス>`） |

> この例は**書式の見本**であり、内容は合意事項ではない。
> **アクター名・オブジェクト名はプロダクトのものに置き換える**（`pd/specs/01-strategy/glossary.md`）。
