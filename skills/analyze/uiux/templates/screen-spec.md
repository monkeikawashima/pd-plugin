---
screen: <画面名。voices の `screen` と同じ表記を使う>
status: 起草 | 合意済み | 未合意 | 要再検証
updated: YYYY-MM-DD
phase: <フェーズ / 未定義>
---

# <画面名>

## 位置づけ

| 項目 | 値 |
|---|---|
| フェーズ | |
| コアオブジェクト | <この画面で主役になる対象物> |
| サブオブジェクト | |
| 対応ストーリー | US-xx, US-yy |

## 判断基準

> **この画面は、いまのフェーズのコアへ最短で導けているか。**

<この問いへの答えを一文で。答えられないなら構造層に戻る>

## 顧客ボイスの引き当て

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/voices.mjs" query --screen <画面名>
```

| ID | type | 内容の要点 | この画面での扱い |
|---|---|---|---|
| VOICE-xxx | pain | … | 解消した / 未解消 |
| VOICE-yyy | positive | … | **壊していないことを確認した** |

> 0件なら「**一次情報なし**」と書き、以降の改善案には「デザイナー起案」と明記する。

## 面積配分

一意に決まる場合はそのまま書く。決まらない場合は**等価に並べる**:

| 案 | 配置 | 前提（この案が正しくなる条件） | 棄却条件 |
|---|---|---|---|
| A | | | |
| B | | | |

**何を計測すれば決まるか**: <計測項目>

## 状態設計（**5つ全て埋める**）

| 状態 | 設計 |
|---|---|
| 初期 | |
| 処理中 | |
| 成功 | |
| **空振り**（処理は成功したが有効な結果が無い） | |
| エラー（処理そのものが失敗） | |

> ⚠️ **空振りを省略しない。** 「0件」「該当なし」「AI が答えを出せなかった」の設計が抜けたまま
> 実装されると、利用者の前に沈黙が残る。エラーとは別物として必ず書く。

## 表層

| 項目 | 値 |
|---|---|
| 使用トークン | <トークン名。hex 直書きが無いこと> |
| コントラスト検算 | 済（`a11y-contrast-guard` / 日付） / **未** |
| 状態を色だけで区別していないか | 確認済 / 未 |

## サンプル値の扱い

| 表示箇所 | 値 | 種別 |
|---|---|---|
| | | **サンプル値** / 実データ |

> サンプル値には必ず「サンプル値」と明記する。明記が無いと後で実測値として引用される。

## 未合意事項

| 論点 | 何が分かれば決まるか | 誰が |
|---|---|---|
| | | |

> `未合意` ステータスのまま実装の根拠にしない。
