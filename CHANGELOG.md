# 変更履歴

pd plugin の版ごとの変更。**判定（`validate.py` のルール）が変わった版は、更新すると今まで通っていたファイルが落ちることがある。** 更新前にここを読む。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、版番号は [セマンティックバージョニング](https://semver.org/lang/ja/)。

- **MAJOR** — 既存の分析データを書き換えないと通らなくなる変更
- **MINOR** — 判定・Skill・コマンドの追加。既存データはそのまま通る
- **PATCH** — 文言修正・不具合修正

> **1.0.0 が最初の安定版です。** それ以前は `0.1.0`〜`0.7.0` として開発中の版であり、
> **利用者はいませんでした**。当時は `1.0.0`〜`2.1.0` と名乗っていましたが、
> 配布物に特定プロダクトの語彙が残り、文書どおりに打てないコマンドがあった状態を
> 「安定版」とは呼べないため、番号を振り直しています。
>
> **旧番号のタグ（`v1.0.0`〜`v2.1.0`）は削除しました。** 過去版の checkout はできません。
> 各版で何をなぜ変えたかの記録は、下にそのまま残しています。

## [1.8.0] — 2026-08-16

### 追加 — モックで見せたものが、実装から落ちなくなる（モック台帳）

**分析データの判定は変わっていません。** 今まで通っていたファイルが落ちることはありません。

モックで合意したのに、実装では**要素が1つ足りない・赤が別の赤になっている・文言が言い換わっている**。原因は、モックの中身を覚えているのが**会話だけ**であることです。会話は次の日には残らず、**落ちたことは落ちた側からは見えません。**

モックと実装の間に、機械が読める1枚を挟みます。

- **`pd/specs/05-surface/mocks/<名前>.md`**（モック台帳）に、文言・色・要素を持たせます
- **`scripts/mock_capture.py`** がモックの HTML から**全部**抜き出します。**要約させません** — 要約はそのたびに拾う量が変わり、落ちた1行は落ちたと気づけません
- **`/pd:mock-ledger`**（新設）が、抽出から埋め方までを案内します
- **`UserPromptSubmit`** — 「実装して」「コードに落として」と言われた時点で、該当する台帳を機械的に指します。**台帳が無ければ作り方を案内するだけで、作業は止めません**
- **`Stop`** — モックを渡したのに台帳が無ければ促します（止めません）

人が埋めるのは2箇所だけです。色をどのトークンに対応させるか（**ここが空だと「赤」で合意したものが実装で別の赤になります**）と、要素の実装の目印（コンポーネント名・`data-testid`）です。

落とすものは**消させません**。`見送り: UXDR-xxx` に書き換えます。消せる状態にすると、落ちたことと落とすと決めたことの区別が消えるためです。件数や日付のように実行時に組み立てる文言は `動的: 理由` にします。

### 追加 — 実装とモック台帳の突き合わせを検証する

台帳の `実装:` に実装先（ファイルかディレクトリ）を書いた時点から、`/pd:validate` が次を見ます。**`未着手` の間は照合しません。**

- **モックの文言が実装に無い**（言い換え・欠落）。実装では長い文言が行をまたぐため、**空白を落として**比べます
- **モックの要素が実装に無い**（実装の目印で探して見つからない）
- **実装が色を直書きしている**（モックの生値をそのまま持ち込んでいる）
- 色がトークンに対応づいていない / そのトークンが `design-tokens.md` に無い
- 状態が `実装` / `見送り: UXDR-…` / `動的: 理由` のいずれでもない（**未記入は「実装した」ではありません**）
- モック・実装先・公開の記載が実物を指しているか

**台帳が無いこと自体は違反ではありません。** モックを作らない画面もあり、その場限りの絵まで台帳を強制すると、やがて全部無視されるためです。

### 追加 — UI・モックを作る前に、そのプロジェクトのデザインシステムを必ず参照する

**分析データの判定は変わっていません。** 今まで通っていたファイルが落ちることはありません。

デザインシステムがあるプロジェクトでも、UI やモックを作るたびに参照されるかは**その場の判断**でした。同じプロジェクトで作ったものが揃わず、「参照してください」と規約に書いても書いた本人しか読みません。

答えを1箇所に固定します。

- **`pd/specs/05-surface/design-system.md`**（現在値・上書き）に、種別・真実の源・参照先を記録します。`design-tokens.md` とは分けています（トークンは「値の正はどのファイルか」、こちらは「コンポーネントの出所は何か」で関心が違う）
- **`/pd:init`** に手順が増えました。`package.json` の依存・`components.json`・`tailwind.config.*`・`.storybook/` から**候補を機械的に検出してから**選ばせます。ゼロから聞くと答えが揺れるためです
- **`/pd:design-system`**（新設）で、あとから決め直せます。乗り換えは init のやり直しではありません
- **`UserPromptSubmit`** — UI を作る／直す／モックの話が始まったとき、上のファイルを読むよう機械的に指します

**未設定でも作業は止めません。促すだけです。** 導入直後や PoC ではデザインシステムが決まっていないほうが普通で、そこで止めると `/pd:init` の直後から作業ができません。「**無し（後で決める）**」も選択肢にあり、選べばそう記録されます（未記入と「無いと決めた」を区別できないと、UI の作業のたびに同じ確認が繰り返されます）。

**この hook は `pd/ledger.json` の目印を見ます**（モックの publish とは逆）。`/pd:init` していないリポジトリで「決めろ」と促しても、記録する場所がないためです。

### 追加 — 参照先が実物を指しているかを検証する

`pd/specs/05-surface/design-system.md` がある場合だけ、次を見ます。**無いこと自体は違反ではありません。**

- 「種別」と「真実の源」があるか
- 「参照先」節に書いたパスが**実在するか** — 乗り換えたのに台帳が取り残されると、全員が存在しないディレクトリを参照しに行きます
- 雛形の山括弧（`<パス>`）が埋まらないまま残っていないか
- 「適用しない範囲」に期限の無い例外（`未定`）が残っていないか

URL・パッケージ名（`@…`）・空白を含む起動コマンドは実在を確かめられないため対象外です。

`validate.py` は `hook.py` からこの仕組みが消えていないかも見ます。

## [1.7.0] — 2026-08-16

### 追加 — モックを頼んだら、必ず公開されたページ（Artifact）で返す

**分析データの判定は変わっていません。** 今まで通っていたファイルが落ちることはありません。

「モックを作って」と頼んだとき、公開されたページ（Artifact）のリンクで返るか、手元に HTML が書かれて終わるかが、**その場の判断に委ねられていました。** 同じ頼み方をしても結果が変わり、利用者から見て何が条件なのか読めない状態でした。

hook で挟んで固定します。

- **`UserPromptSubmit`** — 発言に「モック」「モックアップ」「ワイヤー」「画面案」「画面イメージ」「デザイン案」「たたき台」「プロトタイプ」等が含まれたとき、`artifact-design` を読んで **`Artifact` で publish し URL を返すまでが完了**、という指示を機械的に注入します
- **`Stop`** — モックを頼まれ、`.html` を書き、`Artifact` を呼んでいない状態で終わろうとしたら、**一度だけ差し戻します**

**この2つだけは `pd/ledger.json` の目印を見ません。** plugin を入れた全プロジェクトで効きます。`/pd:init` していないリポジトリでモックを頼むことは普通にあり、目印で縛ると一番効いてほしい場面で効かないためです。**当たったときしか何も出さない**ので、無関係なリポジトリで通知が増えることはありません。

止めたいときは「ローカルだけに作って」「公開しないで」と添えてください。促されません。`Artifact` が使えない環境では、理由が1行返って終わります（差し戻しは `stop_hook_active` を見て1回で打ち切ります）。

「API のモック」「テストダブル」は対象外です。

### 追加 — この仕組みを外せないようにした

`validate.py` が次を見ます。**仕組みごと外す穴を塞ぐためです。**

- `hooks.json` に `UserPromptSubmit` の hook があるか
- `hook.py` に `Artifact` の promotion が残っているか
- `hook.py` に差し戻しの打ち切り（`stop_hook_active`）があるか

## [1.6.2] — 2026-08-16

### 変更 — 複数人で使うときの取り決めを、初期化時の規約に足した

**判定は変わっていません。** 今まで通っていたファイルが落ちることはありません。

`/pd:init` が利用プロジェクトの `CLAUDE.md` に書く規約に、機械では担保できない3点を足しました。

- **手元の判定は参考、CI が正。** 版は人によってズレます（サードパーティの marketplace は auto-update が既定で無効）。合否は `.github/workflows/validate.yml` の `ref` で固定した版で決まります
- **`--accept` を含む変更は、書いた本人以外が見る。** 承認は `pd/ledger-log.md` に残りますが、表記の統一を超えていないかは人しか判断できません
- **PR に `pd/` の差分が混ざるのは仕様。** 実装と同じコミットで動かすため、別のリポジトリや外部ツールに分けません

**既に稼働しているプロジェクトには自動では入りません。** `/pd:init` は既存の「pd の規約」節に触らないためです。必要なら手で追記してください。

## [1.6.1] — 2026-08-14

### 追加 — 適用開始日が、配った版の日付と一致しているかを見る

**利用者への影響はありません。** 開発時の判定です。

判定ごとの適用開始日（`RULE_SINCE`）は「その判定を配った版の日付」であるべきですが、打ち間違いやどの版にも対応しない日付を書いても、これまで何も起きませんでした。**早すぎれば過去の Note を巻き込み、遅すぎれば当分どの Note にも効かない** — どちらも静かに狂います。

CHANGELOG の見出しの日付と照合し、一致しなければ違反にします。

## [1.6.0] — 2026-08-14

### 追加 — 「正しい書き方の例」が無い判定を、違反にするようになった

**利用者への影響はありません。** 変わったのは開発時の規約と自己テストです。

自己テストは「壊れた例を検出できること」ばかりを確かめていて、**「正しく書いたものを落とさないこと」をほとんど確かめていませんでした**（壊れた例が正しい例の 7 倍近くありました）。その結果、1.5.2 と 1.5.3 は**判定を締めた直後に誤検知**を出しています。

- 文書の種類ごとに `expect_ok "…" check_xxx`、Note の判定ごとに `expect_rule` / `expect_no_rule` を要求する。**欠けていれば plugin 自身の検証が落ちます**
- 要求する対象は、振り分け（`validate` / `check_spec`）から**機械で数え上げます**。一覧を手で持たないので、文書の種類を足しても更新漏れが起きません
- 不足していた 13 種類ぶん（Note / 計測計画 / 検証計画 / レビュー結果 / 予測データ / 用語集 / Product Context と、Note の 6 判定の正しい例）を追加しました

**本質は件数ではなく内訳です。** 「壊れた例」に偏っていた自己テストに、「正しい例」が同じ数だけ並ぶようになりました。

## [1.5.3] — 2026-08-14

### 修正 — 正しく書いてあるのに警告が出る箇所が2つありました

どちらも**誤検知**です。誤検知は「validate を通すための記述」を生むので、機能の不足より優先して直します。

- **Segment の宣言表**が、内容に数値も定型語（`分解済み` / `Unknown`）も無いと宣言と認められませんでした。`| 新規/既存 | 取得済み | 既存のほうが継続しやすい傾向 |` のような書き方が落ちます。**表の性格は見出し（`Segment` / `セグメント`）で読む**ようにしました。無関係な表に軸名を並べただけでは通らない点は変わりません
- **`ref:` をコメント内に書いていると**、それを実際の指定と取り違えていました（`# 以前は ref: v1.0.0 だった` と書いてあると、正しく上げてあっても「版が古い」と警告）。コメントを読まないようにしました

## [1.5.2] — 2026-08-14

### 修正 — 反例として書いた行が違反になっていた（1.5.0 の退行）

`✗` を付けて「こう書かない」と示した行が、そのまま違反として拾われていました。

```
| 予約者が迷う | ✗ ユーザーが迷う |   → ⚠ 「ユーザー」を単独で使っている
| ✗ 年齢 42歳 | 判断に効かない |      → ⚠ 装飾属性「年齢」がある
```

1.5.0 で否定表現の語彙リストを捨てたとき、同じ定数に入っていた `✗` `❌` まで一緒に捨てたのが原因です。**否定表現は開いた集合なので捨てるべきでしたが、記号は閉じた集合なので残すべきでした。**

### 修正 — 適用開始日の書き忘れが、検出されるようになった

1.5.1 で判定ごとの適用開始日（`RULE_SINCE`）を足しましたが、**そこへ書き忘れても何も起きませんでした。** 仕組みに見えて、実態は「気をつける」に戻っていました。

- `check_new_rules` の中で `err()` / `warn()` を直に呼ぶと、`validate.py` 自身が違反にする
- 判定は `note_err` / `note_warn` で書く。rule 名が要り、rule 名は `RULE_SINCE` に無ければ違反になる。**判定を足す道が、日付を書く道しか無くなりました**

### 修正 — 判定が3つ、意図より緩い / 通じない状態でした

- **`ref` が commit やブランチのとき**、数字らしき部分を版として比較していました（`ref: 4762a00` → `v4762` として「手元より新しい」）。版として読めない場合は「版を固定していない」と伝えます
- **Segment の宣言**は、状態を伴う表の行だけを認めます。用語集の表に軸名を並べただけでは通りません
- **`必須 Segment: A / B / C` のように `/` で区切っていた定義**は、1.5.0 から1つの軸として扱われます。黙って壊れないよう、Product Context 側で警告するようにしました

### 変更 — `/pd:uninstall` の対象を明記

`pd/voices/taxonomy.json`（pd 固有の設定）を仕組み側として消します。`pd/specs/01-strategy/glossary.md` は利用者が書くものなので `--purge` でのみ消すことを明記しました。

## [1.5.1] — 2026-08-14

### 修正 — あとから足した判定が、既存の Note に遡って適用されていた

1.5.0 の移行案内は「既存の Note は書き換え不要」と書いていましたが、**実際には遡って適用されていました。** 適用開始日（`RULES_FROM`）が「規約の運用を始めた日」1つしか無く、**判定を足すたびにその日以降の全 Note が新しい判定の対象になる**構造だったためです。

書いた時点で存在しない判定で過去の記録が警告されると、**通すために過去を書き換える**ことになります。追記のみの記録では、これがいちばんやってはいけないことです。

- 判定ごとの適用開始日（`RULE_SINCE`）を持たせた。必須 Segment の宣言は **2026-08-14 以降の Note にだけ**適用する
- それより前の Note に出ていた「必須 Segment『…』の宣言が無い」は消えます。**表を追記する必要はありません**

## [1.5.0] — 2026-08-14

### 変更 — 必須 Segment の判定を「散文に語が出るか」から「表で宣言したか」へ

**この版で警告の出方が変わります。** 今まで本文に Segment 名が出ているだけで通っていた Note が警告され、逆に「データが無いので分解できない」と書いた Note は通るようになります。

旧実装が測っていたのは**語が本文のどこかに出現するか**で、分解したかではありませんでした。そのため「経過月数では分解できていない」という否定文でも通り、誠実に引けない理由を書いた Note は落ちていました。**後者が起きると、語を1つ置けば警告が消えます。それは規律ではなく作文です。**

- Note 側は、必須 Segment ごとに**表の行**で状態を宣言する。散文で触れるだけでは分解したことにならない
- **`Unknown` ＋理由は正規の状態**として通す。理由の無い `Unknown` だけを警告する
- Product Context 側の定義を **frontmatter の構造化リスト**（`id` / `label` / `values`）にした。旧形式の1行も引き続き読む
- 旧形式の分割から ASCII の `/` を外した。`新規/既存` が2つの軸に割れていた。括弧の内側でも切らない（`店舗セグメント（A / B）` が壊れていた）

**移行:** 2026-08-14 以降に書く Note から適用する（1.5.1 で修正。1.5.0 は既存 Note にも遡って適用していた）。表の形は `skills/analyze/framework/kpi.md` を見る。

### 修正 — 規約文そのものが「ユーザー」「年齢」の判定で落ちていた

除外の判定が禁止表現の語彙（`書かない` `禁止` …）を当てにいっていたため、「使わない」で書かれた規約文が漏れていました。実際に `specs/01-strategy/glossary.md`（**呼称を決める当のファイル**）が落ちていました。

- **語彙リストをやめ、use / mention で判定する。** 対象語を括弧・引用符・コードスパンで囲んだ行は「言及」として除外する。語尾が何であっても効く
- `specs/01-strategy/glossary.md` は呼称の判定の対象外にした（規約を所有するファイルを、その規約で裁かない）
- 色の値（HEX）の検査はこれまでどおり。`` `#7c3aed` `` と書くのが普通なので、引用除外を入れると検査が効かなくなる

### 追加 — タグの push で GitHub Release を自動作成する

`release.sh --no-push` で止めた場合や手で `git push origin vX` した場合、Release が作られませんでした（v1.4.0 で実際に漏れた）。Release の本文は起動時の更新通知に出るため、**作り忘れた版は何が変わったか届きません。**

- `.github/workflows/release.yml` を追加。タグの push という1点に寄せ、どの経路で配っても作られるようにした
- 既にある場合は何もしない（`release.sh` が先に作っていることがある）

## [1.4.0] — 2026-08-14

### 追加 — CI が固定している plugin の版と、いま動いている版のズレを検出する

**更新すると警告が出ることがあります（止まりません）。** `.github/workflows/validate.yml` の `ref:` が今の版と違う場合、`⚠` が1件出ます。`ref` を新しい版に書き換えれば消えます。

`ref` は `/pd:init` が走った時点の版を焼き込む文字列で、以後は誰も触らない。`/pd:update` は「`ref` も上げるように伝える」だけで、実際に上げるのは人の記憶に委ねられていた。**忘れると手元（更新済み）と CI（旧版）で違う判定器が動くが、そのズレを誰も見ていなかった** — 実際に v1.0.1 のまま v1.3.0 まで放置され、その間ずっと `✓ 規約違反なし` が出ていた。

- `validate.py` が `ref:` と `plugin.json` の `version` を突き合わせる。古い / 新しい / 固定していない の3通りを警告する
- **エラーにはしない。** 更新直後に全プロジェクトが一斉に落ちると、直す前に検証器のほうが外される
- `/pd:update` の手順を「伝える」から「その場で `ref` を書き換え、警告が消えるまで確かめる」に変えた

## [1.3.0] — 2026-08-14

### 変更 — 設計文書を「再構築の指示書」から「判断の記録」へ畳んだ（`pd-skill-blueprint.md` → `DECISIONS.md`）

**利用者への影響はありません。** 変わったのは配布物側の文書と、開発時の規約です。

`pd-skill-blueprint.md` は「Skill を別プロジェクトで再構築するための唯一の指示書」という位置づけだったが、**23スキル中22スキルの中身が書かれていなかった**。v1.2.0 で UI/UX の22スキルと `skills/analyze/uiux/` を足したとき、この文書だけが `/pd:analyze` 1つ分のまま取り残されている。

気づけなかった理由は、指す仕組みがどこにも無かったこと。`CLAUDE.md` の同期表・`hook.py` の促し・`validate.py` のいずれも `skills/analyze/` の3ファイルしか見ておらず、**規約どおり作業しても埋まらない**状態だった。

- 1,019行 → 206行。**実物（`skills/` `scripts/` `commands/`）を見れば分かることを捨て、実物から復元できない「なぜそう決めたか」だけを残した**（論点表・実際に踏んだ失敗）
- ファイル名を `DECISIONS.md` に改めた。`blueprint` のままだと「作り方が書いてあるはず」と読まれ、同じ肥大が再発する
- `CLAUDE.md` の8行の同期表を、3つの条件（論点を決めた / 判定を緩めた / 失敗を仕組みで塞いだ）に置き換えた
- PostToolUse hook の促しを組み替えた。**判定・仕組み**（`validate.py` / `hook.py` / `hooks.json` / `release.sh`）を触ったときは理由の追記を、**配布物**（`skills/` `commands/`）を触ったときは README の同期を促す。いずれも plugin 自身のパスに限る（利用プロジェクトの `skills/` で誤検知していた形を正した）

### 追加 — 起動時の外部通信を、恒久的に止められるようにした

pd は起動時に配布元へ新しい版を問い合わせている（版番号を読むだけで、プロジェクトの内容は一切送らない）。v1.2.1 まで、これを止める手段は `hooks/hooks.json` の SessionStart を外すことだけで、`/plugin update` のたびに黙って復活していた。**恒久的に止められないなら、止められないのと同じ。**

- 環境変数 **`PD_UPDATE_CHECK=0`**（`false` / `off` / `no` も可）で、問い合わせも起動時の案内も行わなくなる
- 設定が plugin の外にあるため、**`/plugin update` で戻らない**。shell の rc にも `.claude/settings.json` の `env` にも書ける
- 止まるのは更新確認だけ。規約の検証（編集直後・ターン終了）はこれまでどおり動く
- `/pd:update` は、止めている場合その旨を伝えたうえで手動更新を実行する。**黙って無効だと、更新が無いのか止めているのか区別できない**

### 追加 — 判定2件

- **更新確認を止める入口が消えていないか** — 消えると `/plugin update` のたびに外部通信が復活し、SECURITY.md の記述が嘘になる
- **止め方が SECURITY.md に書かれているか** — 止められることを知らせなければ、無いのと同じ

自己テストは133件 → 138件（v1.3.0 時点の数。現在の数は `sh scripts/selftest.sh` が出す）。

## [1.2.1] — 2026-08-14

### 追加 — 更新通知に「何が変わったか」が出るようになった

今までは「新しい版がある」とだけ出て、中身は CHANGELOG を開くまで分からなかった。**判定が変わった版では、更新すると今まで通っていたファイルが落ちる。** その警告を出しておきながら、何が変わったかをその場で見せていなかった。

- `release.sh` が **GitHub Release を自動で作る**ようになった。本文は CHANGELOG の該当セクションから組み立てる（人が新しく書くものは無い）
- 本文は「`###` 見出しを並べた要約」＋「セクション全文」の順。通知には先頭8行しか出ないため、**要約が先に来ないと長い箇条書きの途中で切れる**
- 作成に失敗しても**配布は止めない**。Release が無い場合はタグへフォールバックする実装が既にあるため、通知そのものは出る

### 変更 — README に「何ができるか」を足した

冒頭の「調査・意思決定・UI/UX 改善・効果検証」が、実際にできることの一部しか指していなかった。**ペルソナ・ジャーニー・探索・文言・アクセシビリティが1語も現れていない**状態で、20ある Skill のうち4〜5個ぶんしか読み取れなかった。

- `何が起きるか` の手前に **`何ができるか`** を新設し、11段階（探索／記録／戦略／分析／要件／構造／画面／見た目／評価／検証／決定）を一覧にした
- 「**まだ無いものを作るときにも使える**」を明記した。「改善」という語が、既存物を直す道具だという読みを生んでいた
- 「材料が足りなくても止まらない」を冒頭側にも置いた

## [1.2.0] — 2026-08-14

### 追加 — 声が無い段階・競合・Figma を扱えるようにした

- **`ux-heuristic-eval`（骨格）** — 声がまだ無い画面の網羅点検。原典の10原則に pd の3つ（**空振り / 待ち / 入口の存在**）を足した13観点を「違反あり / 違反なし / **判定不能**」で必ず埋める。重大度は印象で決めず、頻度・影響・回避を埋めてから4段階に落とす。**出力は全て `Interpretation`。このスキルに `Fact` は存在しない。** 声があるなら `ux-design-review` へ回す
- **`competitive-analysis`（戦略）** — 調べることではなく**仕分けること**が本体。`strategy-insight-writer` と同じ A（相場）/ B（差別化）/ C（賭け）を共用し、**2社以上が持てば必ず A**。B に置くには声か実測の差が要る。触れた度合いを `実測 / 目視 / 伝聞 / 未確認` で明示し、**`未確認` の製品には何も書かない**。`voices/` に出てこない製品は競合として扱わない。機能一覧の比較表は作らない
- **`figma-sync`（表層）** — 仕様と Figma のズレを一覧にする。**既定は「突き合わせのみ」**で、方向（仕様 → Figma / Figma → 仕様）が決まるまで書き込まない。Figma 側に無い**空振り・エラー・処理中**の状態を必ず見る。書き込み時は variable 束縛・Light mode・`loadFontAsync`・Auto Layout 保持・detach 禁止・実画像配置を守り、**スクリーンショットで確認するまで完了と報告しない**。`figma-use` スキルの事前読み込みを必須にした

### 変更 — 配り忘れを検出し、版の指定を省けるようにした

CHANGELOG に版を書いたのに `plugin.json` を上げ忘れると、`/plugin update` は "already at the latest version" で止まり、**書いた内容が誰にも届かない**。見た目は完成しているので気づけない（実際に v1.2.0 でこの状態になっていた）。

- `validate.py` が**逆向きの食い違い**を検出するようになった — CHANGELOG の最新の見出しが `plugin.json` より新しい場合に落ちる
- `release.sh` の版指定を省けるようにした。**`sh scripts/release.sh` だけで CHANGELOG の最新の見出しの版を配る**。どの版にするかは CHANGELOG を書いた時点で決まっており、同じ判断を2回させると片方だけ間違える

### 追加 — 外部ソース（Notion 等）を読んだときの扱いを決めた

MCP 連携がある環境では Notion / Drive / Slack の URL をそのまま渡せるが、**リンク先はその場限りで消える**。今までは「Context に反映するかどうか」が利用者の申し出待ちで、黙って上書きされることも、何も残らないこともあった。

- **鮮度は自動で判定する** — 利用者に「いつのものか」を言わせない。MCP のレスポンスに含まれる更新日時（Notion `last_edited_time` / Drive `modifiedTime` / Slack `ts`）を読み、分析対象の期間と比較して「最新の仕様書」「古い資料」へ自動で振り分ける。振り分け結果は一覧で見せる（黙って格下げしない）。聞くのは**取得できなかった場合だけ**で、その資料は現在値の根拠に使わない
- メタデータと本文の「最終更新」表記が食い違う場合は**古い方**を採る（新しく見せる方に倒さない）。利用者が「これが最新」と言った場合も、更新日が対象期間より古ければ食い違いを伝えてから使う
- **反映時** — 外部ソース由来の値で `products/{product-name}.md` を書き換える場合、**差分を提示して「すべて反映 / 選んで反映 / 反映しない」を出す**。会話・実測・リポジトリ由来の変化は従来どおりそのまま反映する
- 上書きには変更前の値を併記する。既存の実測値を、更新日が不明な外部ページの値で既定選択のまま上書きしない
- `Past Experiments` / `Decisions` は追記のみ。外部ソースを根拠に過去の決定や棄却結果を書き換えない

### 追加 — 5層すべてに担当スキルを置いた

v1.1.0 まで、**戦略層と骨格層には判定後の行き先が無かった**。`ux-layer-triage` が「戦略の話です」「骨格の話です」と判定しても渡す先が無く、議論は好みに戻っていた。特に骨格層は `templates/screen-spec.md` だけが所有者不在で置かれ、これから作る画面まで `ux-design-review`（実装後のレビュー）へ流れていた。**空振り状態の欠落を実装後に指摘しても、作り直しにしかならない。**

- **`strategy-insight-writer`（戦略）** — インサイト / コンセプト / ポジショニングの言語化。コンセプトを要素に分解して番号を振り、下流の還元テスト（R1）が一文で参照できる形にする。「競合にあるから」は必ず「相場(A)」に分類し、差別化(B)として記録させない。

- **`user-persona-builder`（戦略）** — 利用者像。**創作ではなく `pd/voices/` からの集計**。`speaker_role` 単位で引き当て、根拠件数 `n` と期間・確信度（実証 / 作業仮説 / 未取得）・非対象を必須にする。`n = 0` の系統は像を書かず「未取得」で停止する。年齢・性別などの装飾属性は持たせない。

- **`ux-journey-mapper`（要件）** — 時間軸。**5層は静的な断面で、順序も所要時間も表さない。** ジャーニーのステップは `kpi.md` の主要タスク時間の構成要素と**同一物**として扱い、二重に定義しない。裏側（バックステージ）は任意の行。

- **`ux-screen-spec-writer`（骨格）** — 画面仕様を**実装より前に**書く。5状態（初期 / 処理中 / 成功 / **空振り** / エラー）を必ず埋め、決まらない配置は等価案として並べる。色の値は書かない（トークン名だけ）。

- **`design-token-keeper`（表層）** — トークン台帳と逸脱一覧の所有者。定義の**真実の源**（どのファイルが正か）を固定し、命名の衝突を確認し、逸脱を期限つきで記録する。**値の最終決定は DESIGN のまま。**

テンプレートを3つ追加した（`persona.md` / `journey.md` / `design-tokens.md`）。

### 追加 — 層の中の空白を埋めた

5層に担当を置いたあと、**層の内側に残っていた4つの空白**を埋めた。

- **`ux-research-planner`（探索）** — `ux-validation-planner` は**仮説の採否**を決める設計で、「まだ何が課題か分からない」段階を扱えなかった。探索の失敗は「仮説が決まらない」ではなく**何も新しく分からない**こと。最大の原因である誘導質問を排すため、「**聞かないこと**」を先に決めさせる。打ち切りは人数ではなく飽和で決め、飽和しなければ「飽和していない」と明記させる。置き場所は `pd/validations/` に `RP-` 接頭辞で同居させた（**分けると片方が忘れられる**）。

- **`a11y-interaction-guard`（骨格）** — a11y はコントラストしか見ていなかった。キーボードで完了できるか・フォーカスの順序と戻り先・読み上げに必要な情報・44px・動きの抑制を検査する。`a11y-contrast-guard` と分けたのは、**あちらは値を計算し、こちらは動かして確かめる**ため（1つに束ねると、色を変えるだけの場面で操作の項目が並び、毎回スキップされる）。確かめられなかった項目は「未確認」と書かせる。

- **`ia-navigation-reviewer`（骨格）** — 「タブを増やしたい」が構造層のスキルへ流れて詰まっていた。各項目を**対象物かジャーニーのステップに対応づけ**、どちらにも対応しないものは追加せず上流へ返す（「その他」「設定」の下を、決めなかったものの置き場にしない）。**到達経路0本＝迷子**の検出を必須にした。

- **`ux-writing-guard`（骨格）** — 用語集で呼称を決めているのに、UI 文言がそれに従っているかを見る仕組みが無かった。エラー文に**次の行動**があるか、0件・初回・権限なしを書き分けているか、ボタンが**押した結果**を書いているかを点検する。用語集に無い語が UI に出ていたら、それは文言の問題ではなく**構造層が未決着**なので戻す。

テンプレートを2つ追加した（`research-plan.md` / `navigation.md`）。`rules/04-accessibility.md` に色以外の下限（キーボード / フォーカス / 読み上げ / 寸法 / 動き）を追加した。

### 判定の追加

**既存のプロジェクトのファイルが新たに落ちることはない。** 追加した判定は、いずれも v1.2.0 で初めて置き場所が決まったファイル（`personas.md` / `journeys.md` / `04-skeleton/screens/` / `design-tokens.md`）だけを見る。

- **ペルソナ** — 確信度が無い / 根拠（`VOICE-…`）も「未取得」も無い / 「作業仮説」なのに棄却条件が無い / 「非対象」が無い、を違反にした。装飾属性（年齢・性別・趣味・家族構成・年収）は**警告**に留めた（業種によっては正当な分解軸になりうる。誤検知で判定器全体が無視されるほうが損失が大きい）。
- **ジャーニー** — 主要タスク時間との対応が無い / 対象ペルソナが無い / 根拠も「一次情報なし」も無い / 所要時間に値も「未取得」も無い、を違反にした。
- **画面仕様** — 5状態の欠落（**特に空振り**）/ 根拠も「デザイナー起案」も無い / 色の値の直書き（`#rrggbb`）、を違反にした。
- **トークン台帳** — 「真実の源」が無い / 逸脱一覧が無い / 逸脱の行に「未定」がある（**期限の無い例外は恒久化する**）、を違反にした。
- **スキルの形**（`check_skill_shape`）— 各 `skills/*/SKILL.md` に `name` とディレクトリ名の一致・`description`・**役割分界の表**を要求した。スキル名を列挙して守る方式は必ず取り残されるため、形だけを縛る。

`specs/` の判定は層ごとに1〜4個に絞った。`specs/` は人が手で直す現在値であり、判定を増やすと「validate を通すための記述」が書かれ始める。**それは規律ではなく作文になる。**

- **探索計画**（`RP-`）— 対象者の欄が無い / 「聞かないこと」が無い / 逐語の記録先が無い、を違反にした。検証計画（`VP-`）の判定はそのまま残る。
- **ナビゲーション** — 到達経路が無い / 各項目の対応先（対象物・ステップ）が無い、を違反にした。
- **呼称** — `specs/` 内で「ユーザー」を単独で使っている箇所を**警告**にした。v0.6.0 から規約にあったが誰も見ていなかった判定。複合語（ユーザーストーリー / ユーザー検証 …）を弾かないよう、直後が助詞・句読点・行末のときだけ拾う。

自己テストを31件追加した（合計128件）。

### 変更

- `ux-layer-triage` の振り分け表を層ごとに整理し、**骨格層を「これから作る（`ux-screen-spec-writer`）」と「既に在る（`ux-design-review`）」に分けた**。
- `ux-update-cascade` にトリガー E（ペルソナの増減・確信度の変化）/ F（ジャーニーのステップの変化）/ G（トークンの値・命名の変化）を追加した。
- `rules/00-common-language.md` に「5層が持たない軸 — 時間」の節を追加し、各層に担当スキルを明記した。

## [1.1.0] — 2026-08-14

### 追加

- **`/pd:update` を追加した。** 更新は2段階（配布元の一覧の取り直し → plugin の更新）だが、**順序を間違えると更新されないどころか古い版へ戻る**。実測: 手元の一覧が v1.0.0 のとき `claude plugin update pd@pd-plugin` は「updated from 1.0.1 to 1.0.0」と表示して**降格させた**。`plugin update` は手元にある一覧しか見ないため。利用者に順序を覚えさせず、1つのコマンドにまとめた。

- **新しい版が出ていると、Claude Code の起動時に知らせるようになった**（`scripts/update_check.py`）。版の番号（手元 → 最新）・その版の変更内容・`/pd:update`・変更履歴とリリースへのリンクを `SessionStart` で出す。

  **更新は放っておくと届かない。** Claude Code はサードパーティの配布元を既定で自動更新しないため、版を上げても利用者の手元は古いままになり、手元（更新済み）と CI（旧 `ref`）で判定が食い違う。知らせる場所が無いのが原因だった。

  起動は待たせない。起動時はキャッシュを読むだけで、配布元への問い合わせは裏で走らせ、結果は次の起動から使う（間隔は6時間）。**取れなくても黙る** — 圏外・社内プロキシ・API 制限で失敗しても、案内が出ないだけで作業は妨げない。案内が出るのは `pd/ledger.json` のあるプロジェクトだけで、pd と無関係のリポジトリでは何もしない。

### 判定の追加

- `scripts/update_check.py` の欠落と、`hook.py` からの呼び出しの消失を違反にした。**知らせる仕組みは、外されると誰も気づかない。**
- `commands/update.md` の欠落を違反にした。名前空間の無い `/update` という表記も違反にした（実際に使えるのは `/pd:update`）。

## [1.0.1] — 2026-08-13

### 修正

- **`check_repo_layout()` が pd と無関係のファイルを違反として拾っていた。** リポジトリ全体（`.git` / `node_modules` / `.local` 以外）を走査し、`.png` / `.jpg` / `.sql` / `.csv` を「生データ」、`.DS_Store` を「不要ファイル」と判定していたため、**アプリのソースと同居しているリポジトリでは `/pd:init` の直後から数百件の違反が出た**（実例: `public/` の画像・`supabase/migrations/*.sql`・`tests/visual/*.png` で 218 件。clean checkout の CI でも 86 件残る）。

  pd が責任を持つのは pd が作ったものだけ。走査対象を `pd/`（`BASE`）配下に限定した。旧レイアウト（root 直下に `products/` `analyses/`）では `BASE == ROOT` なので従来どおり全体を走査する。plugin 自身の検証（`SELF_CHECK`）も従来どおり全体を見る。

  **常に赤い判定器は誰も見なくなる。** 判定者を1つに保つには、その判定者が管轄外で落ちないことが要る。

## [1.0.0] — 2026-08-13

**配布物から特定プロダクトの語彙と、元プロジェクトの実装への参照を取り除いた。**

plugin は「思考の型」を配るもので、特定プロダクトのためのものではない。にもかかわらず v0.7.0 まで、飲食予約サービスの業務定義（ゲスト / 店舗スタッフ、予約完了時間、予約対応時間、多言語）が `skills/analyze/uiux/glossary.md` にそのまま入っていた。**用語集は「唯一の定義」を名乗る文書なので、固有の語が混ざると他プロダクトで間違った言葉が唯一の定義として強制される。**

同時に、文書に書かれていたコマンドの一部が**実行できない状態**だった（plugin に `package.json` は無いのに `pnpm voices …` と案内していた）。plugin として完全に機能するように直した。

### 破壊的変更

- **コマンド名から `pd-` の接頭辞を外した。** `/pd:pd` は接頭辞が二重で、何をする道具かも読み取れなかった

  | 旧 | 新 |
  |---|---|
  | `/pd:pd` | **`/pd:analyze`** |
  | `/pd:pd-init` | **`/pd:init`** |
  | `/pd:pd-validate` | **`/pd:validate`** |
  | `/pd:pd-uninstall` | **`/pd:uninstall`** |

  plugin 名（`pd`）は変えていない。**変えると別 plugin 扱いになり、既存利用者に `/plugin update` で更新が届かなくなる**ため。名前空間が読みやすくなる利益より、更新経路を切らないことを優先した

  `skills/pd/` は `skills/analyze/` に、`commands/pd-init.md` は `commands/init.md` に移動した。**利用プロジェクト側のファイルは1つも動かない**（`pd/` 配下の置き場所は変更なし）

  `validate.py` の「名前空間の付け忘れ」判定を作り直した。素の名前が `init` / `validate` という普通の英単語になったため、`scripts/validate.py` や `pd/validations/` を誤検知しないよう、直前が語・`/`・`:` のときは対象外にしている（誤検知する判定は無視されるようになるため）

- **`speaker_role` の enum を廃止した。** これまでの許容値 `guest` / `store-staff` / `store-owner` / `admin` / `unknown` は飲食・小売の語彙で、SaaS・社内ツール・医療・教育では当てはまらない

  代わりに **形式だけ**を縛る（`^[a-z][a-z0-9-]*$`。英小文字・数字・ハイフン）。値は各プロジェクトが `pd/voices/taxonomy.json` の `speaker_role` に列挙し、**列挙した時点でその中に縛られる**（`object` / `phase` と同じ仕組み。新しい概念は増えていない）

  完全な自由記述にしなかったのは、`Staff` / `店舗スタッフ` / `store staff` が同居すると `query --speaker_role` で引けなくなるため。**引けない台帳は無いのと同じ。**

  **移行:** 既存のボイスの `speaker_role` を、そのプロダクトの役割名（例: `end-user` / `operator` / `manager` / `admin` / `unknown`）に置き換える。旧値のうち `guest` `admin` `unknown` は形式上そのまま通るが、**意味が合っているかは別問題**なので見直すこと。`store-staff` / `store-owner` も形式は通る

- **業務用語の置き場所を利用プロジェクト側へ移した。** `skills/analyze/uiux/glossary.md` §3 が持っていた利用者の呼称・主要タスク時間・多言語の定義を削除し、**書き方だけ**を残した

  定義先は `pd/specs/01-strategy/glossary.md`。`/pd:init` が雛形（中身は「未定義」）を作る。**分かっていないことを埋めると、誰も決めていない定義が流通する**ため、雛形は埋めない

### 修正

- **plugin のソースを開いているときに、毎ターン誤検知が出ていた。** 「plugin 自身を見ているのか、plugin を使うプロジェクトを見ているのか」を `ROOT == PLUGIN_ROOT` で判定していたが、**hook 経由では `PLUGIN_ROOT` がインストール済みの複製（`~/.claude/plugins/…`）を指す**ため、パスが一致しない

  結果、plugin のリポジトリ自身が「pd を使うプロジェクト」と誤認され、`.github/workflows/validate.yml` に「plugin を取得する手順が無い」という違反が出続けていた。**plugin 自身の CI に checkout は不要**なので、直しようがない違反である

  判定をマニフェストの `name` の一致で行うようにし、自己検証のときは**いま開いているソース**を見るようにした。**誤検知する判定は無視されるようになる**（`rules/05-operations.md` §5「検証が常に赤い」）ため、放置できない

- **UI/UX の成果物の置き場所の案内が、検証器の期待と食い違っていた。** スキルの案内どおりに書くと落ちる状態だった

  | | 案内していた形 | 検証器の期待 |
  |---|---|---|
  | UXDR | `pd/decisions/UXDR-…md` | `pd/decisions/{プロダクト}/{年}/UXDR-…md` |
  | 検証計画 | `pd/validations/VP-…md` | `pd/validations/{プロダクト}/{年}/VP-…md` |
  | 計測計画 | `pd/measurements/MP-…md` | `pd/measurements/{プロダクト}/{年}/MP-…md` |
  | レビュー結果 | `pd/reviews/DR-…md` | `pd/reviews/{プロダクト}/{年}/DR-…md` |

  加えて `templates/uxdr.md` の frontmatter に必須の `product` が無く、**テンプレートをそのままコピーして埋めても落ちた**

  v0.6.0 で置き場所を `{プロダクト}/{年}/` に変えたとき、検証器と `skills/analyze/SKILL.md` は直ったが、UI/UX 側のスキル5本とテンプレート2本が取り残されていた。**書いた人には、どちらが正しいか判断する材料が無い**（`rules/05-operations.md` §6 と同じ症状）

- **`pnpm voices …` を `node "${CLAUDE_PLUGIN_ROOT}/scripts/voices.mjs" …` に直した**（6ファイル）。plugin に `package.json` は無く、書かれたとおりに実行すると必ず失敗していた
- **廃止済みの `voices index` の案内を削除した。** v0.6.0 で廃止したコマンドが `user-voice-ledger` に残っていた
- **存在しないファイルへの参照を解消した** — `.devin/rules/uiux-ops.md`（「停止してよい6箇所」の実体。**規約が読めない状態だった**ので、内容を `rules/01-layer-discipline.md` §4 に書き起こした）、`pd/voices/SCHEMA.md`、`.claude/skills/…`
- **特定の技術スタックへの依存を外した** — `src/i18n/routing.ts` / `src/app/[locale]/globals.css` / `.next` / `pnpm build`。a11y の「実ビルド出力の hex で検算する」という原則は残し、取り方はプロジェクトに委ねた。Tailwind v4 の `@theme` の話は、その例として明示的にスコープを付けた
- 例文から業種を抜いた（`店舗B-2` → `運用者B-2`、「ネット予約比率」→「該当操作の完了率」、記入例のアクター / オブジェクト名、`実名・店名` → `実名・組織名`）
- `voices.mjs` の usage が案内するスキーマのパスが誤っていた

### 追加

- **`validate.py` に `check_plugin_is_generic()`** — 配布物に固有物が混ざっていないかを見る。これが無かったため、今回の混入が v0.7.0 まで残った

  ①**外部プロジェクトのパス参照**（`src/` `.devin/` `.next` `pnpm ` `.claude/skills/`）— 存在しないパスは必ず壊れるので網羅的に効く
  ②**業種語の辞書** — 反例として並べている行（`✗` `❌`「書かない」を含む行）は対象外

  ⚠️ **②は辞書であり、当たった分しか見えない。空振りしても「固有物が無い」証明にはならない。** 判定を足すのは実際に混入を見つけたときで、想像で業種を増やさない

- **`validate.py` に `check_documented_paths()`** — 文書が案内する記録の置き場所に `{プロダクト}/{年}` が入っているかを見る。上の食い違いを二度と起こさないため。ディレクトリだけへの言及（`pd/decisions/` にある、等）は対象外

- **`/pd:init` が `pd/voices/taxonomy.json` と `pd/specs/01-strategy/glossary.md` を作る。** どちらも空（未定義）で作る。埋まるまでは形式チェックだけが働く（導入直後を赤くしないため）
- **`validate.py` が `taxonomy.json` を見るようになった。** これまで taxonomy の検査は `voices.mjs` にしか無く、CI（`validate.py` のみ実行）を素通りしていた。`object` / `phase` / `speaker_role` の3つを見る
- 自己テストに16件追加（`speaker_role` の形式違反・大文字混入 / taxonomy の有無と一致 / 配布物への業種語・外部パス・存在しない npm script の混入 / 案内する置き場所の取り残し / 反例の行とディレクトリへの言及は通す / 文書側から形式が消えると検出する / 素の `/init` と旧名 `/pd-init` の両方を拾い、パスへの言及は誤検知しない / 別パスの validate.py からでも自己検証になる）。合計 93 件
- `schema-sync.py` は `speaker_role` が enum でなくなったため、代わりに**形式の正規表現が3箇所に揃っているか**を突き合わせる

## [0.7.0] — 2026-08-13

### 追加

- **`skills/pd/uiux/rules/05-operations.md`** — 導入から時間が経つと現れる劣化と対処。**仕組みが死ぬのは壊れるときではなく、使われなくなるとき。**壊れたら検証器が止めるが、使われなくなっても検証器は何も言わない（対象が増えないだけなので通り続ける）。スキルが呼ばれなくなる / 台帳が更新されない / 決定が記録されない / spec が古いまま進む / 検証が常に赤い / 規約と検証器がずれる、の6症状を挙げる

- **`scripts/schema-sync.py`** — ボイスのスキーマが `voice-schema.md` / `voices.mjs` / `validate.py` の3箇所で一致しているかを機械的に突き合わせる。CI と `release.sh` の関門に追加した

  規約を書いた文書と判定するコードは別々に育つ。片方だけ変えると「文書どおりに書いたのに弾かれる」事故になり、**現場にはどちらが正しいか判断する材料が無い**。実際に前身の実装で `source` の許容値が文書と CLI でずれていた。注意書きでは防げないため機械化した

- 自己テストに3件追加（正常時に通る / CLI 側だけ enum を足すと検出する / 文書側だけ enum を減らすと検出する）。合計 77 件

## [0.6.0] — 2026-08-13

**UI/UX の作業を pd に統合した。**これまで UI/UX の規律（層の判定・顧客ボイス台帳・デザインレビュー・アクセシビリティ検算）は各プロジェクトが `uiux/` のような独自ディレクトリで持っていた。pd と併用すると**同じ事実を二箇所に書く**ことになり、必ず片方が古くなる。plugin を入れるだけで UI/UX の改善まで回せるようにした。

### 破壊的変更

- **ボイスの書式を全面的に変えた。** 1ファイル = 1機会（面談単位）から **1ファイル = 1声**（論点単位）へ。

  ```
  旧: pd/voices/{product}/{年}/YYYYMMDD-{取得経路}.md   frontmatter 4キー
  新: pd/voices/{product}/{年}/VOICE-NNN-{slug}.md      frontmatter 12キー必須
  ```

  変えた理由は**引き当て**。UI を直すときに必要なのは「その画面について誰が何と言ったか」で、機会単位でまとめると `screen` で引けない。**引けない台帳は無いのと同じ。**

  ⚠️ **既存の voices は通らなくなる。**移行するか、取り直す。新しい書式は `skills/pd/uiux/voice-schema.md`。

- **`voices.mjs index` を廃止した。**索引ファイルは更新漏れで実態と乖離する（pd の既存規約）。一覧は `query` / `stats` で取る。

### 追加

- **UI/UX スキル10種** — `ux-layer-triage`（層の判定。UI の話が出たら最初に使う）/ `user-voice-ledger` / `user-story-writer` / `object-model-reviewer` / `a11y-contrast-guard` / `ux-design-review` / `ux-validation-planner` / `ux-measurement` / `ux-decision-record` / `ux-update-cascade`

- **ボイス台帳 CLI** — `scripts/voices.mjs`。依存パッケージ0（Node 標準モジュールのみ）。

  ```
  node "${CLAUDE_PLUGIN_ROOT}/scripts/voices.mjs" query --screen <画面名>
  ```

  **UI を作る・直す前に必ず実行する。**0件は「問題が無い」ではなく「記録されていない」。

- **置き場所を5つ追加** — `pd/specs/`（層ごとの現在値。上書き）/ `pd/decisions/`（UXDR）/ `pd/validations/`（VP）/ `pd/measurements/`（MP）/ `pd/reviews/`（DR）。後ろ4つは**追記のみ**。そのとき何を決めたかを後から書き換えると、判断の経緯が消えるため。

- **判定の追加** — UXDR に「決めなかったこと」（何が分かれば決まるか／いつまでに／誰が の3列）と「影響範囲の変わらなかったもの」を必須にした。作業仮説には**観測可能な閾値を持つ棄却条件**を必須にした。レビュー結果には根拠ボイスか「デザイナー起案」の明記を必須にした。

- **規律とテンプレート** — `skills/pd/uiux/rules/`（共通言語・層の規律・決定規則・証拠規則・アクセシビリティ）、`skills/pd/uiux/templates/`（画面仕様・ユーザーストーリー・UXDR・検証計画）、`glossary.md`。

### 設計上の注意

pd（問題 → 原因 → 仮説 → 検証 → 決定）と UI/UX の5段階（戦略 → 要件 → 構造 → 骨格 → 表層）は**直交する**。前者は推論の流れ、後者は抽象度の階層。同じ事実を両方に書かないため、**層ごとの現在値は `pd/specs/`、推論の履歴は `pd/analyses/`** と置き場所で分けている。

## [0.5.0] — 2026-08-13

### 変更

- **プロジェクト側の置き場所を `pd/` 配下にまとめた。** `products/` `analyses/` `voices/` `simulations/` `.local/` が root 直下に散っていたため、**やめたいときにどれが pd のものか判別できなかった**。今後 `/pd:pd-init` は `pd/` の中だけに作る（`pd/products/` `pd/analyses/` …）。`pd/` の外に出るのは `CLAUDE.md` と `.github/workflows/validate.yml` の2つだけ

  **既に使っているプロジェクトは、そのままで通る。移動は不要。** 検証器が root 直下の旧レイアウトも読み続ける。移したい場合の手順は README の「置き場所を移す」にある。**移しても台帳の承認は要らない**（記録のキーを根からの相対パスにしたため）

  ⚠️ 新旧を混在させないこと。`pd/analyses/` と `analyses/` が両方あると、`pd/` 側だけが検証対象になり、root 直下は素通りする

- `/pd:pd-init` が `.gitignore` に書く行が `.local/` から `pd/.local/` になった。旧レイアウトのプロジェクトで書き換える必要は無い

### 追加

- **`/pd:pd-uninstall`** — プロジェクトに作ったものを一覧・削除する。plugin のアンインストール（`/plugin uninstall`）はプロジェクト側のファイルに触れないため、片付けはこのコマンドが受け持つ

  ```
  /pd:pd-uninstall                一覧表示のみ（何も削除しない）
  /pd:pd-uninstall --keep-data    仕組みだけ消す。分析結果は残す
  /pd:pd-uninstall --purge        分析結果ごと全部消す
  ```

  **plugin より先に実行する**（plugin を外すとこのコマンドも消える）。`CLAUDE.md` / `.gitignore` / CI は pd が足した行・節だけを消し、ファイルごとは削除しない

- `validate.py` の判定: `commands/pd-uninstall.md` が配布物に含まれていること
- `selftest.sh` に旧レイアウトの検証を追加（root 直下でも通ること・hook が動くこと）。61 → 63 件

## [0.4.0] — 2026-08-13

### 追加

- `scripts/release.sh` — 版を上げて配る手順を1コマンドに畳んだ。**未コミットのままタグを打つとタグが古い内容を指す**事故が3回続いたため、手順ではなく仕組みで止める。作業ツリーが汚れていれば中止し、版の書き換え（`plugin.json` / `marketplace.json` / `pd-init` の `ref`）と検証を済ませてからコミット・タグ・push まで行う
- `validate.py` の判定: `scripts/release.sh` が配布物に含まれていること

## [0.3.0] — 2026-08-13

### 修正

- **文書のコマンド名が誤っていた。** 正しくは `/pd:pd` / `/pd:pd-init` / `/pd:pd-validate`。plugin のスキルは Claude Code が必ず「plugin名:スキル名」に名前空間化するため、**文書どおりに `/pd-init` と打った利用者は「コマンドが無い」で終わっていた**
- `validate.py` の判定を追加: 文書に名前空間の無いコマンド名が書かれていないこと
- **`/pd:pd-init` が生成する CI の `ref` が `v0.1.0` のままだった。** 新しいプロジェクトが初期状態から旧版の判定器で CI を回していた。あわせて `ref` が現在の版と一致することを `validate.py` の判定に追加

### 変更

- **hook を OS 非依存にした。** `hooks.json` からシェル構文（`case` / `read` / `printf`）と `jq` を排除し、`scripts/hook.py` を呼ぶだけにした。**Windows では sh も jq も無く、hook が丸ごと動かなかった。** 依存は `python3` のみ（`validate.py` が既に要求している）

### 追加

- `scripts/hook.py` — hook の入口。動くべき場面の判定と出力の整形だけを行う。**判定は書かない**（判定者は `validate.py` のまま）
- `validate.py` の判定: `hooks.json` にシェル依存の記述が混ざっていないこと、`hook.py` に目印（`pd/ledger.json`）と blueprint 同期の促しが残っていること
- `selftest.sh` に hook の**挙動**テストを追加（形が揃っていても止められなければ意味がないため）。50 → 58 件

## [0.2.0] — 2026-08-13

### 追加

- `CHANGELOG.md`（このファイル）。版ごとの変更を追えるようにした
- `validate.py` の判定: `CHANGELOG.md` が存在し、`plugin.json` の現在の版が記載されていること。**版を上げて履歴を書き忘れると落ちる**

### 変更

- `README.md` を書き直した。「誰のためのものか」「更新方法」を追加。**更新は自動では届かない**（Claude Code はサードパーティの配布元を既定で自動更新しない）点を明記

## [0.1.0] — 2026-08-12

### 追加

- Product Discovery Skill（`/pd`）を Claude Code plugin として配布開始
- `/pd-init` — 利用プロジェクト側の置き場所・台帳・CI を作る
- `/pd-validate` — 規約違反を手動で確認する
- `validate.py` — 規約の唯一の判定者。置き場所・命名・必須項目・匿名化・元データの混入・過去記録の書き換えを機械的に判定する
- `selftest.sh` — 検証器が本当に違反を検出するかを確かめる自己テスト
- hook（PostToolUse / Stop / SessionStart）— 編集の時点で違反を止め、セッションの開始と終了に残存を通知する
- `pd-skill-blueprint.md` — Skill を別プロジェクトで再構築するための指示書
